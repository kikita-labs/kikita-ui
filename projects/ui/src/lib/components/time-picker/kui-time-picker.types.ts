/** Display/parse format for `input[kuiTimePicker]` and `kui-time-picker-panel`. */
export type KuiTimePickerFormat = '24h' | '12h';

/** AM/PM period, used only when `format` is `'12h'`. */
export type KuiTimePickerPeriod = 'AM' | 'PM';
