/**
 * `kui-pagination` layout preset.
 * - `full`: summary text and rows-per-page picker on top of `compact`.
 * - `compact`: First/Prev/numbers+ellipsis/Next/Last.
 * - `simple`: only Prev/"Page X of Y"/Next, for narrow layouts.
 */
export type KuiPaginationVariant = 'full' | 'compact' | 'simple';
