/**
 * Underline behavior for `[kuiLink]`. Values match MUI `Link`'s `underline` axis by meaning
 * (`none | hover | always`), reused as a proven minimal set rather than inventing a new one.
 */
export type KuiLinkUnderline = 'always' | 'hover' | 'none';
