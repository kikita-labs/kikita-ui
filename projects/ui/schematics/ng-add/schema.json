{
  "$schema": "http://json-schema.org/schema",
  "$id": "KikitaUiNgAdd",
  "title": "Kikita UI ng-add options",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Angular application project to configure."
    },
    "skipProvider": {
      "type": "boolean",
      "description": "Skip adding provideKikitaUi() to app.config.ts.",
      "default": false
    },
    "skipStyles": {
      "type": "boolean",
      "description": "Skip adding the Kikita UI stylesheet path to angular.json.",
      "default": false
    },
    "theme": {
      "type": "boolean",
      "description": "Scaffold the default Ember theme seed configuration in provideKikitaUi().",
      "default": false
    }
  }
}
